{
  "name": "taxi-project",
  "version": "0.1.0",
  "private": true,
  "description": "Site de réservation de taxi en ligne",
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "npm run dev --prefix web",
    "dev:api": "npm run dev --prefix api",
    "build": "npm run build:web && npm run build:api",
    "build:web": "npm run build --prefix web",
    "build:api": "npm run build --prefix api",
    "start": "concurrently \"npm run start:web\" \"npm run start:api\"",
    "start:web": "npm run start --prefix web",
    "start:api": "npm run start --prefix api",
    "lint": "npm run lint --prefix web && npm run lint --prefix api",
    "db:migrate": "npm run db:migrate --prefix api",
    "db:studio": "npm run db:studio --prefix api",
    "db:generate": "npm run db:generate --prefix api",
    "db:push": "npm run db:push --prefix api",
    "setup": "npm install && cd web && npm install && cd ../api && npm install",
    "clean": "rimraf node_modules web/node_modules api/node_modules web/.next api/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
